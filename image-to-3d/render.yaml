services:
  - type: web
    name: image-to-3d
    runtime: python
    rootDir: image-to-3d
    buildCommand: pip install -e .
    startCommand: uvicorn img3d.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
